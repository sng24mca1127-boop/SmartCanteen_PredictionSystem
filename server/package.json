{
  "name": "sngce-canteen-server",
  "version": "1.0.0",
  "description": "Backend server for SNGCE Canteen Management System",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "init-db": "node -e \"require('./db.js').initDb().then(() => console.log('Database initialized')).catch(console.error)\""
  },
  "keywords": ["canteen", "management", "sngce", "food", "ordering"],
  "author": "SNGCE Canteen Team",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "bcryptjs": "^2.4.3",
    "sqlite3": "^5.1.6",
    "sqlite": "^4.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}

